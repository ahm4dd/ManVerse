{
  "name": "manverse-desktop",
  "version": "1.0.1",
  "private": true,
  "type": "commonjs",
  "main": "main.cjs",
  "scripts": {
    "dev": "MANVERSE_DEV=true bun launch.cjs",
    "start": "bun launch.cjs",
    "build": "bun --cwd ../app build && bun ../scripts/prepare-runtime.ts && bun ../scripts/bundle-bun.ts && electron-builder --config electron-builder.json"
  },
  "dependencies": {
    "electron-updater": "^6.1.7"
  },
  "devDependencies": {
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8"
  }
}
