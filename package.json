{
  "name": "manverse",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "dev:api": "turbo run dev --filter=api",
    "docker:dev": "docker compose --env-file ./docker/.env.development -f ./docker/compose.dev.yaml up -d",
    "docker:dev:down": "docker compose --env-file ./docker/.env.development -f ./docker/compose.dev.yaml down -v",
    "docker:test": "docker compose --env-file ./docker/.env.test -f ./docker/compose.test.yaml up -d",
    "docker:test:down": "docker compose --env-file ./docker/.env.test -f ./docker/compose.test.yaml down -v",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "test:cov": "turbo run test:cov",
    "test:e2e": "turbo run test:e2e",
    "test:e2e:watch": "turbo run test:e2e:watch",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "setup:api": "turbo run setup --filter=api"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.8.19",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
